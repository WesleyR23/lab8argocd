apiVersion: v1
kind: Service
metadata:
  name: cst407net-service
  namespace: cst407-deployment
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    # this pod port 
  selector:
  # now we choose which pods to load balance
    app: cst407